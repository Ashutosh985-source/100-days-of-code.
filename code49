#include <stdio.h>

int main() {
    int i, j;

    for (i = 1; i <= 5; i++) {               // Outer loop: rows 1 to 5
        for (j = 6 - i; j <= 5; j++) {       // Inner loop: from (6 - i) to 5
            printf("%d", j);
        }
        printf("\n");                        // Newline after each row
    }

    return 0;
}
